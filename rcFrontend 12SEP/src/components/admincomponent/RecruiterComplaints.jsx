// Recruiter version of the complaints page - shares the same UI as MyComplaints
// but is protected by ProtectedRoute and placed under the recruiter navigation.
// We simply re-export the same component for clarity and route isolation.
export { default } from "../components_lite/MyComplaints";
